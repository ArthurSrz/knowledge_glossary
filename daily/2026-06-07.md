Re-reading the submitted article avec [[Data]] [[Design]] and intuitiveness. 

The editors mention clearly that the article goes beyond [[open data]], and should have a broader scope. I am considering then to open it to all [[tabular data]] but making clear the framework is a first step to tackle all [[data type]]s. It sounds like the article has greater value while having a narrower focus on data. But it makes sense with [[Synthetic data generation]] agenda and the idea of `NavigationTree`. 

I really like the problem statement : 

`Tabular datasets, whether published on open data platforms or held within organisations, face a critical challenge: when designed for expert users, they remain inaccessible to non-expert users with varying data literacy levels.`


Now turning to the [[package]] updates and making a production-ready system. I think I need to refactor things, first working on the different parts of the package explained in the paper, and address the bugs editors encountered, which are : 

* I used [[ollama]], but the default model timed out. I went for the simpler llama3.2:1b. This may have limited LLM interpretation*
* Network hosting between a locally running app and a docker container (neo4j) is painful (known Mac thing). I had to run the docker with a "- - network host" config option and ensure ollama was started using "OLLAMA_HOST=0.0.0.0 ollama serve" from the terminal
*  The neo4j from within the streamlit app failed. Looking through app.py, the neo4j password was not set to "password", which was the password in the docker run command. After changing app.py, I could connect.

The first part in the data adaptative structures...but I need to replay the interface here. 